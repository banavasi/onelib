{
	"name": "@banavasi/scripts",
	"version": "0.2.4",
	"description": "Onelib update pipeline and project scripts",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"bin": {
		"onelib-scripts": "./dist/cli.js"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"lint": "biome check .",
		"format": "biome check --write .",
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist .turbo node_modules"
	},
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	},
	"dependencies": {
		"@banavasi/components": "workspace:*",
		"@banavasi/skills": "workspace:*",
		"@banavasi/onelib": "workspace:*",
		"picocolors": "^1.1.1"
	},
	"devDependencies": {
		"typescript": "^5.8.0"
	}
}
