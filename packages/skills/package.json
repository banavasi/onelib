{
	"name": "@onelib/skills",
	"version": "0.1.0",
	"description": "Curated skills and generators",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"lint": "biome check .",
		"format": "biome check --write .",
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist .turbo node_modules"
	},
	"devDependencies": {
		"typescript": "^5.8.0"
	},
	"dependencies": {
		"@onelib/registry": "workspace:*"
	}
}
