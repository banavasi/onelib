import { Meta, Primary, Controls, Story, Source, Canvas } from '@storybook/addon-docs/blocks';
import * as HeroStories from './hero.stories';

<Meta of={HeroStories} />

# Hero Section

Complete hero section with integrated navbar, mobile menu, gradient background, CTA buttons, and statistics. Self-contained with inline SVG icons — no external dependencies required.

- **Source:** [Sera UI](https://seraui.com)
- **Category:** Sections
- **Peer Dependencies:** None

## Preview

<Primary />

## Props

This component is a self-contained section with no configurable props. Customize by editing the component source directly.

## Usage

```tsx
import { HeroSection } from "@/components/sections/hero";

export default function LandingPage() {
  return (
    <main>
      <HeroSection />
    </main>
  );
}
```
